# Generate a long random value and inject it through the process environment.
# Do not commit the real token or pass it as a command-line argument.
CALL_SERVICE_API_TOKEN=replace-with-a-long-random-token
