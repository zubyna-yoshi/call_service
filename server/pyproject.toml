[project]
name = "company-call-directory"
version = "0.1.0"
description = "Excel/CSV-backed caller ID directory API for a small internal beta"
requires-python = ">=3.11"

[project.scripts]
call-directory = "directory_service.cli:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
